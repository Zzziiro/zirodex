import React from "react";

function SearchList() {
  // const { setPokeList, getPokeList, setLoading } = React.useContext(
  //   AuthContext
  // );

  return (
    <>
      <NavBar pokemonFilter={pokemonFilter} />
      <div>SearchList</div>
    </>
  );
}
export default SearchList;
